<UserControl x:Class="SchoolMedicalWpf.App.Nurse.HealthSchedulePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolMedicalWpf.App.Nurse"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#059669"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#D97706"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#DC2626"/>
        <SolidColorBrush x:Key="InfoBrush" Color="#0891B2"/>
        <SolidColorBrush x:Key="LightBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0"/>

        <!-- Button Styles -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="4" ShadowDepth="2"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Compact Card Style -->
        <Style x:Key="CompactCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Padding" Value="16"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
        </Style>

        <!-- Status Badge Style -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <!-- Main Grid with proper proportions -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="100"/>
            <!-- Header: Fixed optimal height -->
            <RowDefinition Height="Auto" MinHeight="180"/>
            <!-- Schedule Panel: Fixed optimal height -->
            <RowDefinition Height="*" MinHeight="400"/>
            <!-- Main Content: Takes remaining space -->
            <RowDefinition Height="Auto" MinHeight="80"/>
            <!-- Action Panel: Fixed optimal height -->
        </Grid.RowDefinitions>

        <!-- Compact Header Section -->
        <Border Grid.Row="0" Style="{StaticResource CompactCardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}" 
                        CornerRadius="12" Width="48" Height="48" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="🏥" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Title and Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Ghi nhận kết quả khám sức khỏe và tiêm chủng" 
                               FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,4"/>
                    <TextBlock Name="txtCurrentDateTime" 
                               Text="2025-07-04 15:13:49 UTC - User: danielleit241" 
                               FontSize="13" Foreground="{StaticResource SecondaryBrush}"/>
                </StackPanel>

                <!-- Status Indicator -->
                <Border Grid.Column="2" Background="#EFF6FF" CornerRadius="8" Padding="12,8" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Hoạt động" FontSize="12" FontWeight="Medium" 
                                   Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Schedule Selection Panel - More Compact -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Schedule Selection Row -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Name="btnSelectSchedule" 
                            Content="📅 Chọn Schedule đang diễn ra" 
                            Background="{StaticResource PrimaryBrush}" 
                            Foreground="White"
                            Style="{StaticResource ModernButtonStyle}"
                            Click="SelectSchedule_Click"/>

                    <TextBlock Grid.Column="1" Name="txtActiveSchedulesCount" 
                               Text="(4 schedule đang diễn ra)" 
                               VerticalAlignment="Center" 
                               Margin="16,0" 
                               Foreground="{StaticResource SecondaryBrush}"
                               FontSize="14"/>

                    <!-- Quick Stats -->
                    <Border Grid.Column="2" Background="{StaticResource LightBrush}" 
                            CornerRadius="8" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Sinh viên" FontSize="12" 
                                       Foreground="{StaticResource SecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Selected Schedule Info - More Compact -->
                <Border Grid.Row="1" Name="pnlSelectedSchedule" 
                        Visibility="Visible"
                        Background="#EFF6FF" 
                        BorderBrush="{StaticResource PrimaryBrush}" 
                        BorderThickness="2" 
                        CornerRadius="8" 
                        Padding="16" 
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Name="txtSelectedScheduleTitle" 
                                       Text="Khám sức khỏe định kỳ đầu năm học (HealthCheck)"
                                       FontWeight="Bold" 
                                       FontSize="16" 
                                       Foreground="#1E40AF"/>
                            <TextBlock Name="txtSelectedScheduleInfo" 
                                       Text="Từ 04/07/2025 đến 14/07/2025 | Đối tượng: Tất cả"
                                       Foreground="{StaticResource SecondaryBrush}" 
                                       Margin="0,2,0,0"
                                       FontSize="13"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Name="btnLoadStudents" 
                                    Content="👥 Tải lại danh sách" 
                                    Background="{StaticResource SuccessBrush}" 
                                    Foreground="White"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Click="LoadStudents_Click" 
                                    Margin="0,0,8,0"/>
                            <Button Name="btnChangeSchedule" 
                                    Content="🔄 Đổi Schedule" 
                                    Background="{StaticResource SecondaryBrush}" 
                                    Foreground="White"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Click="SelectSchedule_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- No Schedule Selected - Hidden by default since we have one selected -->
                <Border Grid.Row="2" Name="txtNoScheduleMessage" 
                        Visibility="Collapsed"
                        Background="#FEF3C7" 
                        BorderBrush="{StaticResource WarningBrush}" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="⚠️" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Chưa chọn schedule. Vui lòng nhấn nút 'Chọn Schedule đang diễn ra' để bắt đầu."
                                   Foreground="#92400E" 
                                   FontWeight="Medium"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content Area - Optimized Space Usage -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <TabControl Name="tcMain" Visibility="Visible" Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" 
                                            Background="Transparent" 
                                            BorderThickness="0,0,0,3" 
                                            BorderBrush="Transparent"
                                            Padding="16,10"
                                            Margin="0,0,4,0">
                                        <ContentPresenter ContentSource="Header" 
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#F8FAFC"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
                    </Style>
                </TabControl.Resources>

                <!-- Students List Tab -->
                <TabItem Header="📋 Danh sách sinh viên">
                    <Grid Margin="0,16,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <DataGrid Name="dgStudents" Style="{StaticResource ModernDataGridStyle}">
                                <DataGrid.Columns>
                                    <!-- Optimized column widths for better proportion -->
                                    <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentCode}" Width="90" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding StudentName}" Width="160" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Lớp" Binding="{Binding Grade}" Width="70" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridCheckBoxColumn Header="Đã thực hiện" Binding="{Binding IsCompleted}" Width="100">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>

                                    <DataGridTemplateColumn Header="Ngày thực hiện" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <DatePicker SelectedDate="{Binding DatePerformed}" 
                                                            IsEnabled="{Binding IsCompleted}"
                                                            Margin="4"
                                                            FontSize="11"
                                                            BorderBrush="{StaticResource BorderBrush}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Trạng thái" Width="130">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Margin="4">
                                                    <Border.Style>
                                                        <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                            <Setter Property="Padding" Value="6,3"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Chưa thực hiện">
                                                                    <Setter Property="Background" Value="#FEF3C7"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã có kết quả">
                                                                    <Setter Property="Background" Value="#DBEAFE"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu kết quả khám">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu kết quả tiêm">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã cập nhật kết quả">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu cơ bản">
                                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Status}" 
                                                               FontSize="10" 
                                                               FontWeight="Medium"
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="140">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                                <Setter Property="FontSize" Value="11"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="Thao tác" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" 
                                                            HorizontalAlignment="Center" 
                                                            Margin="4">
                                                    <!-- Compact buttons -->
                                                    <Button Content="📝" 
                                                            Background="{StaticResource InfoBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Nhập kết quả"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="EnterResults_Click"
                                                            IsEnabled="{Binding IsCompleted}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding IsCompleted}" Value="True"/>
                                                                            <Condition Binding="{Binding HasExistingResults}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <Button Content="👁️" 
                                                            Background="{StaticResource SuccessBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Xem kết quả"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="ViewResult_Click"
                                                            IsEnabled="{Binding HasExistingResults}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasExistingResults}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <Button Content="✏️" 
                                                            Background="{StaticResource WarningBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Chỉnh sửa"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="EditResult_Click"
                                                            IsEnabled="{Binding HasExistingResults}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasExistingResults}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </Grid>
                </TabItem>

                <!-- Results Summary Tab -->
                <TabItem Header="📊 Tổng hợp kết quả">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Compact Statistics Cards -->
                        <Grid Grid.Row="0" Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource CompactCardStyle}" Margin="0,0,8,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#EFF6FF" CornerRadius="8" 
                                            Width="36" Height="36" Margin="0,0,12,0">
                                        <TextBlock Text="👥" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Tổng sinh viên" FontSize="12" 
                                                   Foreground="{StaticResource SecondaryBrush}" Margin="0,0,0,2"/>
                                        <TextBlock Name="txtTotalStudents" Text="0" FontWeight="Bold" FontSize="20" 
                                                   Foreground="{StaticResource PrimaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CompactCardStyle}" Margin="4,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#ECFDF5" CornerRadius="8" 
                                            Width="36" Height="36" Margin="0,0,12,0">
                                        <TextBlock Text="✅" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Đã hoàn thành" FontSize="12" 
                                                   Foreground="{StaticResource SecondaryBrush}" Margin="0,0,0,2"/>
                                        <TextBlock Name="txtCompletedStudents" Text="0" FontWeight="Bold" FontSize="20" 
                                                   Foreground="{StaticResource SuccessBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource CompactCardStyle}" Margin="8,0,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#FEF3C7" CornerRadius="8" 
                                            Width="36" Height="36" Margin="0,0,12,0">
                                        <TextBlock Text="⏳" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Chưa hoàn thành" FontSize="12" 
                                                   Foreground="{StaticResource SecondaryBrush}" Margin="0,0,0,2"/>
                                        <TextBlock Name="txtPendingStudents" Text="0" FontWeight="Bold" FontSize="20" 
                                                   Foreground="{StaticResource WarningBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Results Summary Table -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <DataGrid Name="dgResultsSummary" 
                                      Style="{StaticResource ModernDataGridStyle}"
                                      IsReadOnly="True">
                                <!-- Same optimized columns as the first tab -->
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentCode}" Width="90" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding StudentName}" Width="160" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="Medium"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Lớp" Binding="{Binding Grade}" Width="70" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="FontSize" Value="12"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridCheckBoxColumn Header="Đã thực hiện" Binding="{Binding IsCompleted}" Width="100" IsReadOnly="True">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>

                                    <DataGridTemplateColumn Header="Ngày thực hiện" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <DatePicker SelectedDate="{Binding DatePerformed}" 
                                                            IsEnabled="False"
                                                            Margin="4"
                                                            FontSize="11"
                                                            BorderBrush="{StaticResource BorderBrush}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Trạng thái" Width="130">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Margin="4">
                                                    <Border.Style>
                                                        <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                            <Setter Property="Padding" Value="6,3"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Chưa thực hiện">
                                                                    <Setter Property="Background" Value="#FEF3C7"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã có kết quả">
                                                                    <Setter Property="Background" Value="#DBEAFE"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu kết quả khám">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu kết quả tiêm">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã cập nhật kết quả">
                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Đã lưu cơ bản">
                                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Status}" 
                                                               FontSize="10" 
                                                               FontWeight="Medium"
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="140" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="8,0"/>
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                                <Setter Property="FontSize" Value="11"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="Thao tác" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" 
                                                            HorizontalAlignment="Center" 
                                                            Margin="4">
                                                    <!-- Same compact buttons as first tab -->
                                                    <Button Content="📝" 
                                                            Background="{StaticResource InfoBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Nhập kết quả"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="EnterResults_Click"
                                                            IsEnabled="{Binding IsCompleted}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding IsCompleted}" Value="True"/>
                                                                            <Condition Binding="{Binding HasExistingResults}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <Button Content="👁️" 
                                                            Background="{StaticResource SuccessBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Xem kết quả"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="ViewResult_Click"
                                                            IsEnabled="{Binding HasExistingResults}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasExistingResults}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <Button Content="✏️" 
                                                            Background="{StaticResource WarningBrush}" 
                                                            Foreground="White"
                                                            ToolTip="Chỉnh sửa"
                                                            Width="28" Height="28"
                                                            FontSize="12"
                                                            BorderThickness="0"
                                                            Click="EditResult_Click"
                                                            IsEnabled="{Binding HasExistingResults}"
                                                            Margin="1">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasExistingResults}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>

        <!-- Empty State - Updated visibility and positioning -->
        <StackPanel Grid.Row="2" Name="pnlNoData" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Visibility="Collapsed">
            <Border Background="White" 
                    CornerRadius="16" 
                    Padding="48,32" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="16" ShadowDepth="4"/>
                </Border.Effect>
                <StackPanel HorizontalAlignment="Center">
                    <Border Background="#F1F5F9" CornerRadius="50" Width="64" Height="64" Margin="0,0,0,16">
                        <TextBlock Text="📋" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Chưa có dữ liệu sinh viên" 
                               FontSize="16" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center" 
                               Foreground="#1E293B" 
                               Margin="0,0,0,6"/>
                    <TextBlock Text="Vui lòng chọn schedule và tải danh sách sinh viên để bắt đầu" 
                               FontSize="13" 
                               HorizontalAlignment="Center" 
                               Foreground="{StaticResource SecondaryBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Compact Action Panel -->
        <Border Grid.Row="3" Style="{StaticResource CompactCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Icon -->
                <Border Grid.Column="0" Background="#EFF6FF" CornerRadius="6" 
                        Padding="8,6" Margin="0,0,12,0" VerticalAlignment="Center">
                    <TextBlock Text="💾" FontSize="14"/>
                </Border>

                <!-- Status Text -->
                <TextBlock Grid.Column="1" Name="txtLastUpdate" 
                           Text="Đã tải 2 sinh viên lúc 06:23:34 04/07/2025" 
                           Foreground="{StaticResource SecondaryBrush}"
                           VerticalAlignment="Center"
                           FontSize="13"/>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Name="btnSaveAll" 
                            Content="💾 Lưu tất cả" 
                            Background="{StaticResource SuccessBrush}" 
                            Foreground="White"
                            Style="{StaticResource ModernButtonStyle}"
                            Click="SaveAll_Click" 
                            IsEnabled="True" 
                            Margin="0,0,8,0"/>
                    <Button Name="btnRefresh" 
                            Content="🔄 Làm mới" 
                            Background="{StaticResource SecondaryBrush}" 
                            Foreground="White"
                            Style="{StaticResource ModernButtonStyle}"
                            Click="Refresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>